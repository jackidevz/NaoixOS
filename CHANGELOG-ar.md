# سجل التغييرات (Changelog)

## NaoixSH v0.2.3-patchV2 — 2026-07-19

### إضافات
- أمر جديد `jmdevel` — اسم بديل (alias) لأمر `jackmasterdev`، أُضيف
  لتسهيل الاستخدام. كلا الأمرين يُفعّلان نفس الوظيفة الخفية الخاصة
  بالمطور `run_jackmasterdev()`.
- أمر جديد `easterg` — اسم بديل (alias) لأمر `hidden-easter-egg`،
  أُضيف أيضاً لتسهيل الاستخدام. كلا الأمرين يُنتجان نفس المُخرَج.

### تعديلات
- تحديث تنسيق مُخرَج أمر `ver`.
- تحديث تنسيق مُخرَج أمر `about`.
- ترقية `SHVER` من `v0.2.3-patchV1` إلى `v0.2.3-patchV2` في ملف
  `version.h`.

### ملاحظات
- يبقى `KEVER` (إصدار نواة NaoixOS) عند `v0.6.8-rc1`، دون تغيير في
  هذا التحديث.

